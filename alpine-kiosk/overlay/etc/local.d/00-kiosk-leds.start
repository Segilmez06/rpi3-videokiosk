#!/bin/sh
# Ensure Red PWR LED is off during boot
for led in /sys/class/leds/PWR /sys/class/leds/led1 /sys/class/leds/*pwr*; do
    if [ -d "$led" ]; then
        echo none > "$led/trigger" 2>/dev/null || true
        echo 0 > "$led/brightness" 2>/dev/null || true
    fi
done

